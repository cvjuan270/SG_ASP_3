@model IEnumerable<SG_ASP_3.Models.EspecialidadMedica>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Especialidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Especialidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdEspeMedic }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdEspeMedic }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdEspeMedic })
        </td>
    </tr>
}

</table>
