@model IEnumerable<SG_ASP_3.Models.ExaMedico>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Examen)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Examen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdExMed }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdExMed }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdExMed })
        </td>
    </tr>
}

</table>
